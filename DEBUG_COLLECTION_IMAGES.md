# دليل حل مشاكل صور الكولكشنات 🔧

## المشكلة: الصور مش بتظهر

### الخطوة 1️⃣: تأكد من وجود الصور في قاعدة البيانات

**افتح Supabase → SQL Editor وشغّل:**
```sql
SELECT id, name, slug, image_url, is_featured, is_active, display_type
FROM collections
ORDER BY created_at DESC;
```

**النتيجة المتوقعة:**
- لو `image_url` فيه قيمة (مش NULL) → يجب أن تظهر الصورة
- لو `image_url = NULL` → ستظهر خلفية سوداء

---

### الخطوة 2️⃣: تأكد من is_featured = true

**الكولكشنات تظهر في الصفحة الرئيسية فقط لو:**
```sql
is_featured = true AND is_active = true
```

**لتفعيل جميع الكولكشنات:**
```sql
UPDATE collections
SET is_featured = true, is_active = true;
```

---

### الخطوة 3️⃣: إضافة صور تجريبية (للاختبار السريع)

**شغّل ملف `ADD_TEST_IMAGES_TO_COLLECTIONS.sql`**

هذا سيضيف صور من Unsplash لجميع الكولكشنات للاختبار.

---

### الخطوة 4️⃣: إضافة صورك الخاصة

#### الطريقة الصحيحة (من لوحة الإدارة):

1. **افتح** `/admin/collections`
2. **اضغط تعديل** على الكولكشن
3. **اضغط على منطقة الصورة**
4. **اختر صورة** من جهازك
5. **اضغط تحديث**
6. **Refresh** الصفحة الرئيسية

#### الطريقة اليدوية (SQL):

```sql
UPDATE collections
SET image_url = 'رابط_الصورة_هنا'
WHERE slug = 'اسم_الكولكشن';
```

---

### الخطوة 5️⃣: تأكد من إعدادات Next.js

**الملف:** `next.config.ts`

يجب أن يحتوي على:
```typescript
images: {
  remotePatterns: [
    {
      protocol: 'https',
      hostname: 'images.unsplash.com',
    },
    {
      protocol: 'https',
      hostname: '*.supabase.co',
      pathname: '/storage/v1/object/public/**',
    },
  ],
}
```

---

### الخطوة 6️⃣: أعد تشغيل السيرفر

**في Development:**
```bash
# أوقف السيرفر (Ctrl+C)
npm run dev
```

**امسح الكاش:**
- Ctrl + Shift + R (Windows)
- Cmd + Shift + R (Mac)

---

## الفحص النهائي ✅

### افحص Console في المتصفح:

1. **افتح الصفحة الرئيسية**
2. **اضغط F12** → Console
3. **ابحث عن أخطاء** (errors)

### أخطاء شائعة:

**Error: Invalid src prop**
- ✅ الحل: تأكد من إضافة الـ domain في `next.config.ts`

**Error: 403 Forbidden**
- ✅ الحل: تأكد من صلاحيات Supabase Storage

**الصورة مش بتحمل**
- ✅ الحل: تأكد من صحة الرابط في `image_url`

---

## الاختبار السريع 🚀

**لو عايز تختبر إن الكود شغال:**

```sql
-- أضف صورة تجريبية لكولكشن واحد
UPDATE collections
SET
  image_url = 'https://images.unsplash.com/photo-1556821840-3a63f95609a7?w=800&h=400&fit=crop',
  is_featured = true,
  is_active = true
WHERE id = (SELECT id FROM collections LIMIT 1);
```

ثم refresh الصفحة → يجب أن تظهر الصورة.

---

## النتيجة المتوقعة 📺

### إذا كان `image_url` موجود:
```
┌─────────────────────────┐
│                         │
│   [صورة الكولكشن]       │
│                         │
│   ┌──────────────┐      │
│   │ Track Suits  │      │
│   └──────────────┘      │
└─────────────────────────┘
```

### إذا كان `image_url = NULL`:
```
┌─────────────────────────┐
│                         │
│   خلفية سوداء           │
│                         │
│   ┌──────────────┐      │
│   │ Track Suits  │      │
│   └──────────────┘      │
└─────────────────────────┘
```

---

## لو لسه مش شغال 🆘

**تواصل وابعت:**
1. Screenshot من Console (F12)
2. نتيجة `SELECT * FROM collections`
3. Screenshot من الصفحة الرئيسية
