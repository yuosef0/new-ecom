# حل سريع: الكاردات مش ظاهرة رغم وجود الصور 🚨

## المشكلة
- ✅ الصور موجودة في قاعدة البيانات
- ✅ الصور بتظهر في صفحات الكولكشنات الفردية
- ❌ الكاردات مش بتظهر في الصفحة الرئيسية

## السبب المحتمل

الكاردات **لا تظهر** إلا إذا:
```
is_featured = true
AND
is_active = true
AND
display_type IN ('large', 'small')
```

لو أي شرط من دول مش متحقق، الكارد **مش هيظهر**.

---

## الحل السريع (دقيقة واحدة) ⚡

### الخطوة 1: افتح Supabase → SQL Editor

### الخطوة 2: شغّل السكريبت
انسخ والصق محتوى ملف: **`FIX_CARDS_NOT_SHOWING.sql`**

### الخطوة 3: Refresh الصفحة
- افتح الصفحة الرئيسية
- اضغط Ctrl+Shift+R (Windows) أو Cmd+Shift+R (Mac)

### الخطوة 4: لو لسه مش ظاهرة
```bash
# أعد تشغيل السيرفر
npm run dev
```

---

## شرح السكريبت

السكريبت بيعمل 3 حاجات:

### 1️⃣ الفحص
يعرض لك كل كولكشن وليه مش ظاهر (المشكلة فين).

### 2️⃣ الإصلاح
```sql
-- جعل جميع الكولكشنات مميزة
UPDATE collections SET is_featured = true WHERE is_active = true;

-- إصلاح display_type
UPDATE collections
SET display_type = 'large'
WHERE display_type IS NULL OR display_type NOT IN ('large', 'small');
```

### 3️⃣ التحقق
يعرض لك قائمة الكولكشنات التي ستظهر الآن.

---

## النتيجة المتوقعة

**بعد تشغيل السكريبت:**
- ✅ جميع الكولكشنات النشطة هتبقى مميزة
- ✅ جميع الكولكشنات هيكون لها `display_type` صحيح
- ✅ الكاردات هتظهر في الصفحة الرئيسية
- ✅ الصور هتظهر في الكاردات

---

## التحكم بعد الإصلاح

### لإخفاء كولكشن من الكاردات:
```sql
UPDATE collections SET is_featured = false WHERE slug = 'اسم_الكولكشن';
```

### لتغيير نوع الكارد:
```sql
-- جعله كارد كبير
UPDATE collections SET display_type = 'large' WHERE slug = 'اسم_الكولكشن';

-- جعله كارد صغير
UPDATE collections SET display_type = 'small' WHERE slug = 'اسم_الكولكشن';
```

### من لوحة الإدارة:
- افتح `/admin/collections`
- اضغط زر **⭐ تمييز** للتحكم في `is_featured`
- اضغط **تعديل** لتغيير `display_type`

---

## الفرق بين الإعدادات

| الإعداد | الوظيفة |
|--------|---------|
| `is_featured = true` | يظهر في **الكاردات** بالصفحة الرئيسية |
| `is_active = true` | يظهر في **السايد بار** |
| `display_type = large` | كارد **كبير** (كامل العرض) |
| `display_type = small` | كارد **صغير** (سكرول أفقي) |

---

## ملاحظة مهمة 💡

**لو عايز كولكشن يظهر في الصفحة الرئيسية:**
```sql
is_featured = true    ← مهم جداً!
is_active = true
display_type = 'large' أو 'small'
```

**لو عايز كولكشن يظهر في السايد بار بس:**
```sql
is_featured = false
is_active = true
```

---

## لو المشكلة لسه موجودة 🆘

**افتح Console في المتصفح (F12) وابحث عن:**
- أخطاء في تحميل الصور
- رسائل خطأ من Next.js

**وابعت لي:**
1. Screenshot من Console
2. نتيجة الخطوة 1 من السكريبت (الفحص)
3. Screenshot من الصفحة الرئيسية
