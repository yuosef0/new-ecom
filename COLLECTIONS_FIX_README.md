# إصلاح عدم ظهور الكولكشنات في الكاردات

## المشكلة 🐛
عند إضافة كولكشن جديد، يظهر في السايد بار لكن لا يظهر في الكاردات بالصفحة الرئيسية.

## السبب 🔍
القيمة الافتراضية لحقل `is_featured` في قاعدة البيانات هي `false`، والصفحة الرئيسية تعرض فقط الكولكشنات المميزة (`is_featured = true`).

## الحل النهائي ✅

### الخطوة 1️⃣: تشغيل SQL
افتح **Supabase Dashboard** → **SQL Editor** وشغّل السكريبت:

```sql
-- نسخ محتوى ملف FIX_COLLECTIONS_FEATURED.sql
```

هذا السكريبت سيقوم بـ:
1. تغيير القيمة الافتراضية لـ `is_featured` → `true`
2. تحديث جميع الكولكشنات الموجودة لتصبح مميزة

### الخطوة 2️⃣: التحقق
بعد تشغيل السكريبت:
- افتح الصفحة الرئيسية → ستجد جميع الكولكشنات تظهر في الكاردات ✅
- عند إضافة كولكشن جديد → سيظهر تلقائياً في الكاردات ✅

## كيف تتحكم في الكولكشنات

### من لوحة الإدارة:

1. **لإخفاء كولكشن من الكاردات**:
   - افتح لوحة الإدارة → الكولكشن
   - اضغط على زر النجمة ⭐ لإلغاء التمييز

2. **لإظهار كولكشن في الكاردات**:
   - اضغط على زر النجمة ⭐ لتمييزه

3. **نوع الكارد**:
   - **كارد كبير** (large) → يظهر كامل العرض
   - **كارد صغير** (small) → يظهر في السكرول الأفقي

## ملخص التحكم

| الخيار | الظهور في الكاردات | الظهور في السايد بار |
|--------|---------------------|----------------------|
| `is_featured = true` | ✅ يظهر | - |
| `is_active = true` | - | ✅ يظهر |

**مثال:**
- `is_featured = true` + `is_active = true` → يظهر في الكاردات والسايد بار
- `is_featured = false` + `is_active = true` → يظهر في السايد بار فقط
